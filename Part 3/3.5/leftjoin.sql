-- Inner Joins return records that have matching values in both tables

-- Left Outer Joins return all records from the left table, 
-- and the matched records from the right table

-- Right Outer Joins return all records from the right table, 
-- and the matched records from the left table

-- Full Outer Joins return all records when there is a match in either left or right table


-- In the Yarn/Vendor Table, it is a left join. It selects all the records from the Vendor
-- table first and joins it with the yarns to make the left join.